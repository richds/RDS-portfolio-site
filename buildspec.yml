version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 10
  build:
    commands:
      - node --version
artifacts:
  files:
    - index.html
    - favicon.ico
    - styles/main.css
    - images/*